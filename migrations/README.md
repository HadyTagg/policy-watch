# Migrations

Database migrations live in this directory.

## Usage

Apply the schema by running the application (it bootstraps the SQLite schema automatically) or use the SQL files in this
folder to initialize a new database.
